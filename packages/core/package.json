{
  "name": "@agentry/core",
  "version": "0.0.0",
  "type": "module",
  "description": "Agentry core — event model, config, sandbox, interception, cassettes, assertions, runner.",
  "exports": { ".": "./src/index.ts" },
  "publishConfig": {
    "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } }
  },
  "scripts": { "build": "tsup src/index.ts --format esm --dts --clean" },
  "dependencies": {
    "expect": "^29.7.0",
    "zod": "^3.24.1"
  }
}
